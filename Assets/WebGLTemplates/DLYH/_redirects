# Cloudflare Pages redirect rules for DLYH WebGL
# Ensures OAuth callback routes to the SPA

# OAuth callback - serve index.html (SPA handles the hash fragment)
/auth-callback    /index.html    200

# Catch-all for SPA routing
/*    /index.html    200
