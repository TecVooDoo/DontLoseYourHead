using UnityEngine;

namespace UIColorSystem
{
    /// <summary>
    /// Attribute to make a field read-only in the Unity Inspector
    /// </summary>
    public class ReadOnlyAttribute : PropertyAttribute
    {
    }
} 